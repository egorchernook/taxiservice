package client;

import java.util.*;
import controllers.*;


public class ClientCollection {
        implements Addable<>,
        Deletable<>,
        Findable<> {
    private Map<Long,> = new HashMap<>();
    }
}
